// import type { Row } from '@tanstack/table-core';

export interface PostsDataTable {
	postTitle: string;
	tags: string;
	published: boolean;
	createdAt: string;
	publishedAt: string;
	view: string;
	edit: string;
}
